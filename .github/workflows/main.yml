name: Required Check

on:
  pull_request:
    branches: [ main ]

jobs:
  required-check:
    runs-on: ubuntu-latest
    steps:
    - name: Always pass check
      run: echo "This check always passes"
